Instructions to open Heroku status:

- From server directory, install dependencies (npm install)

- From server directory, run server.js with node js (node server.js)

- From client directory, open index.html
